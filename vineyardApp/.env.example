CV_SERVICE_URL=http://localhost:8000

# PostgreSQL: если сервер слушает стандартный 5432, задайте DB_PORT=5432
DB_PORT=5433
# Имя БД по умолчанию — vineyard_app; после клонирования один раз: bin/rails db:create && bin/rails db:migrate
# DB_NAME=vineyard_app

RAILS_URL=http://localhost:3000

BACKEND_URL=http://localhost:3001

MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=vineyard-videos
MINIO_REGION=us-east-1
